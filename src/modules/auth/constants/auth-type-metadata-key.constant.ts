/**
 * The metadata key used to store the authentication type (e.g., PUBLIC, AUTHENTICATED)
 * set by the @Auth decorator. It is later retrieved by the AppAuthGuard to determine
 * access rules.
 */
export const AUTH_TYPE_METADATA_KEY = 'auth_type_metadata_key';
